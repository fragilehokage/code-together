# Optional override for API base URL (leave empty for same-origin / Vite proxy)
# VITE_API_URL=

# WebSocket URL for local development (Vite dev server does not proxy WS)
VITE_WS_URL=ws://localhost:4050/ws
